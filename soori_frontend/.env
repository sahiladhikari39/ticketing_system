# Relative, not absolute. The host site proxies /support/api through to
# Django, so calls stay same-origin -- no CORS, and cookies work.
# Reach the app at http://localhost:3000/support/login (the host site),
# not at :5173 directly, or these calls have nothing to proxy them.
VITE_API_BASE_URL=/support/api
